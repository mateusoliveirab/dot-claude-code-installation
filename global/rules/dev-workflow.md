---
description: Development workflow and tooling guidelines
---

# Dev Workflow

## Dev Server

- Do not run `npm run dev` — the user usually already has the dev server running. Test directly on the local URL.

## Git Workflow

- Use `git` CLI commands (not `gh` CLI) for all git operations
- Skills available: `/git-commit`, `/git-pr` for streamlined workflows
